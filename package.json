{
  "name": "lumen-landing-page",
  "version": "1.0.0",
  "private": true,
  "description": "Static single-page marketing site for Lumen (hero, features, contact form).",
  "scripts": {
    "preview": "npx --yes serve .",
    "typecheck:js": "node --check scripts/main.js && node --check scripts/contact.js && node --check server/contact-handler.example.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}