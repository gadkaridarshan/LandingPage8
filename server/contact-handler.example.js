After deploying, verify:

- The page loads at your custom domain with the Lumen hero visible.
- The features grid displays correctly in a three-column layout.
- The contact form submits (or falls back to `mailto:`) without errors.